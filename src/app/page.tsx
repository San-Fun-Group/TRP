// Middleware redirects authenticated users to /agent or /reception
// and unauthenticated users to /login — this page is never rendered directly.
export default function RootPage() {
  return null
}
